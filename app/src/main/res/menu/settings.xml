<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:android="http://schemas.android.com/apk/res/android">
    <item  android:id="@+id/refresh"
        android:title="Refresh"
        android:icon="@drawable/refresh_icon"
        app:showAsAction="ifRoom" />

    <item android:id="@+id/search"
        android:title="Search"
        android:icon="@drawable/search_icon"
        app:showAsAction="ifRoom" />

    <item android:id="@+id/add"
        android:title="Add"
        android:icon="@drawable/ic_baseline_add_reaction_24"
        app:showAsAction="never" />

    <item android:id="@+id/help"
        android:title="Help"
        android:icon="@drawable/ic_baseline_help_24"
        app:showAsAction="never" />

    <item android:id="@+id/update"
        android:title="Update"
        android:icon="@drawable/ic_baseline_system_update_24"
        app:showAsAction="never">

        <menu>
            <item android:id="@+id/available"
                android:title="Update Completed"
                android:icon="@drawable/ic_baseline_stop_24"/>

            <item android:id="@+id/unavailable"
                android:title="Update is pending"
                android:icon="@drawable/ic_baseline_pending_24"/>

        </menu>
    </item>
    <item android:id="@+id/setting"
        android:title="Setting"
        android:icon="@drawable/ic_baseline_settings_24"
        app:showAsAction="never" />

</menu>